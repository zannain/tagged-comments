import { HighlightMentionsDirective } from './highlight-mentions.directive';

describe('HighlightMentionsDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightMentionsDirective();
    expect(directive).toBeTruthy();
  });
});
